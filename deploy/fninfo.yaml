apiVersion: o6s.io/v1alpha1
kind: Function
metadata:
  name: fninfo
  namespace: openfaas-fn
spec:
  name: fninfo
  image: stefanprodan/fninfo:0.0.4
  environment:
    namespace: "openfaas-fn"
  limits:
    cpu: "2000m"
    memory: "256Mi"
  requests:
    cpu: "10m"
    memory: "64Mi"
